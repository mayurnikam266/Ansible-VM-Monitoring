<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
      min-height: 100vh;
      padding: 40px 20px;
      color: #ffffff;
      position: relative;
      overflow-x: hidden;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(ellipse at top, rgba(120, 119, 198, 0.3) 0%, transparent 50%),
                  radial-gradient(ellipse at bottom, rgba(255, 54, 128, 0.2) 0%, transparent 50%);
      pointer-events: none;
      z-index: -1;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      position: relative;
      z-index: 1;
    }

    .header {
      text-align: center;
      margin-bottom: 50px;
      position: relative;
    }

    .header::before {
      content: '';
      position: absolute;
      top: -20px;
      left: 50%;
      transform: translateX(-50%);
      width: 100px;
      height: 4px;
      background: linear-gradient(90deg, #ff3672, #7c73e6, #00d4aa);
      border-radius: 2px;
    }

    h1 {
      font-size: clamp(32px, 5vw, 48px);
      font-weight: 700;
      background: linear-gradient(135deg, #ff3672 0%, #7c73e6 50%, #00d4aa 100%);
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 15px;
      letter-spacing: -0.02em;
    }

    .subtitle {
      font-size: 18px;
      color: rgba(255, 255, 255, 0.7);
      font-weight: 300;
      margin-bottom: 20px;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 50px;
    }

    .metric-card {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      padding: 30px;
      text-align: center;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .metric-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
      animation: shimmer 2s infinite;
    }

    @keyframes shimmer {
      0% { transform: translateX(-100%); }
      100% { transform: translateX(100%); }
    }

    .metric-card:hover {
      transform: translateY(-5px);
      background: rgba(255, 255, 255, 0.08);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    .metric-icon {
      font-size: 32px;
      margin-bottom: 15px;
      display: block;
    }

    .metric-value {
      font-size: 28px;
      font-weight: 700;
      color: #ffffff;
      margin-bottom: 8px;
      font-family: 'JetBrains Mono', monospace;
    }

    .metric-label {
      font-size: 14px;
      color: rgba(255, 255, 255, 0.6);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-weight: 500;
    }

    .table-container {
      background: rgba(255, 255, 255, 0.03);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 24px;
      padding: 0;
      overflow: hidden;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: transparent;
    }

    th {
      background: linear-gradient(135deg, rgba(124, 115, 230, 0.3), rgba(255, 54, 114, 0.3));
      color: #ffffff;
      padding: 20px 16px;
      font-size: 14px;
      font-weight: 600;
      text-align: left;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    td {
      padding: 20px 16px;
      font-size: 14px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      transition: all 0.2s ease;
    }

    tr:hover td {
      background: rgba(255, 255, 255, 0.03);
    }

    tr:last-child td {
      border-bottom: none;
    }

    .hostname-cell {
      position: relative;
    }

    .hostname a {
      color: #00d4aa;
      text-decoration: none;
      font-weight: 600;
      font-family: 'JetBrains Mono', monospace;
      transition: all 0.2s ease;
      position: relative;
    }

    .hostname a::after {
      content: '';
      position: absolute;
      bottom: -2px;
      left: 0;
      width: 0;
      height: 2px;
      background: linear-gradient(90deg, #00d4aa, #7c73e6);
      transition: width 0.3s ease;
    }

    .hostname a:hover {
      color: #7c73e6;
      transform: translateX(2px);
    }

    .hostname a:hover::after {
      width: 100%;
    }

    .usage-cell {
      min-width: 160px;
    }

    .usage-value {
      font-family: 'JetBrains Mono', monospace;
      font-weight: 600;
      font-size: 16px;
      margin-bottom: 12px;
    }

    .progress-container {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      height: 8px;
      overflow: hidden;
      margin-bottom: 12px;
      position: relative;
    }

    .progress-bar {
      height: 100%;
      border-radius: 12px;
      transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .progress-bar::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
      animation: progress-shine 1.5s infinite;
    }

    @keyframes progress-shine {
      0% { transform: translateX(-100%); }
      100% { transform: translateX(100%); }
    }

    .cpu-progress { background: linear-gradient(135deg, #ff6b6b, #ff8787); }
    .memory-progress { background: linear-gradient(135deg, #4ecdc4, #45b7aa); }
    .disk-progress { background: linear-gradient(135deg, #ffe66d, #ffcc02); }

    .status-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.3px;
    }

    .status-healthy {
      background: linear-gradient(135deg, rgba(0, 212, 170, 0.2), rgba(0, 212, 170, 0.1));
      border: 1px solid rgba(0, 212, 170, 0.3);
      color: #00d4aa;
    }

    .status-warning {
      background: linear-gradient(135deg, rgba(255, 230, 109, 0.2), rgba(255, 230, 109, 0.1));
      border: 1px solid rgba(255, 230, 109, 0.3);
      color: #ffe66d;
    }

    .status-critical {
      background: linear-gradient(135deg, rgba(255, 107, 107, 0.2), rgba(255, 107, 107, 0.1));
      border: 1px solid rgba(255, 107, 107, 0.3);
      color: #ff6b6b;
    }

    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    .status-healthy .status-dot { background: #00d4aa; }
    .status-warning .status-dot { background: #ffe66d; }
    .status-critical .status-dot { background: #ff6b6b; }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    .footer {
      text-align: center;
      margin-top: 60px;
      padding: 30px;
      background: rgba(255, 255, 255, 0.03);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      position: relative;
    }

    .footer-content {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 15px;
      flex-wrap: wrap;
    }

    .timestamp {
      font-family: 'JetBrains Mono', monospace;
      color: rgba(255, 255, 255, 0.8);
      font-size: 14px;
      font-weight: 500;
    }

    .footer-icon {
      font-size: 20px;
      color: #7c73e6;
    }

    @media (max-width: 768px) {
      .summary-grid {
        grid-template-columns: 1fr;
      }
      
      table {
        font-size: 12px;
      }
      
      th, td {
        padding: 12px 8px;
      }
      
      .metric-card {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🚀 VM Health Dashboard</h1>
      <p class="subtitle">Real-time Infrastructure Monitoring & Analytics</p>
    </div>

    <div class="summary-grid">
      <div class="metric-card">
        <span class="metric-icon">🖥️</span>
        <div class="metric-value">{{ collected_metrics | length }}</div>
        <div class="metric-label">Virtual Machines</div>
      </div>
      <div class="metric-card">
        <span class="metric-icon">🔥</span>
        <div class="metric-value">{{ ((collected_metrics | map(attribute='cpu') | map('float') | sum | float) / (collected_metrics | length)) | round(1) }}%</div>
        <div class="metric-label">Average CPU</div>
      </div>
      <div class="metric-card">
        <span class="metric-icon">📊</span>
        <div class="metric-value">{{ ((collected_metrics | map(attribute='mem') | map('float') | sum | float) / (collected_metrics | length)) | round(1) }}%</div>
        <div class="metric-label">Average Memory</div>
      </div>
      <div class="metric-card">
        <span class="metric-icon">💾</span>
        <div class="metric-value">{{ ((collected_metrics | map(attribute='disk') | map('float') | sum | float) / (collected_metrics | length)) | round(1) }}%</div>
        <div class="metric-label">Average Disk</div>
      </div>
    </div>

    <div class="table-container">
      <table>
        <thead>
          <tr>
            <th>🌐 Hostname</th>
            <th>🔥 CPU Usage</th>
            <th>📊 Memory Usage</th>
            <th>💾 Disk Usage</th>
          </tr>
        </thead>
        <tbody>
          {% for vm in collected_metrics %}
          <tr>
            <td class="hostname-cell">
              <a href="http://{{ vm.hostname }}" target="_blank">{{ vm.hostname }}</a>
            </td>
            <td class="usage-cell">
              <div class="usage-value">{{ vm.cpu }}%</div>
              <div class="progress-container">
                <div class="progress-bar cpu-progress" style="width: {{ vm.cpu }}%"></div>
              </div>
              {% if vm.cpu|float < 50 %}
                <span class="status-badge status-healthy">
                  <span class="status-dot"></span>
                  Healthy
                </span>
              {% elif vm.cpu|float < 80 %}
                <span class="status-badge status-warning">
                  <span class="status-dot"></span>
                  Warning
                </span>
              {% else %}
                <span class="status-badge status-critical">
                  <span class="status-dot"></span>
                  Critical
                </span>
              {% endif %}
            </td>
            <td class="usage-cell">
              <div class="usage-value">{{ vm.mem }}%</div>
              <div class="progress-container">
                <div class="progress-bar memory-progress" style="width: {{ vm.mem }}%"></div>
              </div>
              {% if vm.mem|float < 50 %}
                <span class="status-badge status-healthy">
                  <span class="status-dot"></span>
                  Optimal
                </span>
              {% elif vm.mem|float < 80 %}
                <span class="status-badge status-warning">
                  <span class="status-dot"></span>
                  Monitor
                </span>
              {% else %}
                <span class="status-badge status-critical">
                  <span class="status-dot"></span>
                  High
                </span>
              {% endif %}
            </td>
            <td class="usage-cell">
              <div class="usage-value">{{ vm.disk }}%</div>
              <div class="progress-container">
                <div class="progress-bar disk-progress" style="width: {{ vm.disk }}%"></div>
              </div>
              {% if vm.disk|float < 50 %}
                <span class="status-badge status-healthy">
                  <span class="status-dot"></span>
                  Spacious
                </span>
              {% elif vm.disk|float < 80 %}
                <span class="status-badge status-warning">
                  <span class="status-dot"></span>
                  Monitor
                </span>
              {% else %}
                <span class="status-badge status-critical">
                  <span class="status-dot"></span>
                  Full
                </span>
              {% endif %}
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="footer">
      <div class="footer-content">
        <span class="footer-icon">⚡</span>
        <span class="timestamp">Report generated on {{ timestamp }}</span>
        <span class="footer-icon">📈</span>
      </div>
    </div>
  </div>
</body>
</html>

